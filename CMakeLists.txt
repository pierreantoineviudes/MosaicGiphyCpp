cmake_minimum_required(VERSION 2.8)
project( processRawDataMultithread )
set(nlohmann_json_DIR ~/dev/json/)
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
include_directories( ~/dev/json/ )
add_executable( processRawDataMultithread processRawDataMultithread.cpp )
# CMakeLists.txt
find_package(nlohmann_json 3.2.0 REQUIRED)
# add_library(processRawDataMultithread ...)
target_link_libraries( processRawDataMultithread PRIVATE nlohmann_json::nlohmann_json)
target_link_libraries( processRawDataMultithread ${OpenCV_LIBS} )